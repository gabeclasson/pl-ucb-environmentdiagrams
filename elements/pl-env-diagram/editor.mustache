<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <div class="ExecutionVisualizer {{#show_controls}}ExecutionVisualizerActive{{/show_controls}}">
    <div class="vizLayoutTd" id="vizLayoutTdSecond">
    <div id="dataViz">
        <table id="stackHeapTable">
          <tbody><tr>
            <td id="stack_td">
              <div id="globals_area">
                <div id="stackHeader">Frames</div>
                {{#frame}}
                  <div class="stackFrame removable" id="frame-{{frameIndex}}">
                    {{#return}}
                    {{#show_controls}}<button class="btn removeButton" type="button"></button>{{/show_controls}}
                    <label class="frameHeader">f{{frameIndex}}: </label><input class="frameHeader pl-html-input" id="frame-{{frameIndex}}-name" name="frame-{{frameIndex}}-name" value="{{name}}">
                    <label> [Parent = <input class="frameParentHeader pl-html-input" id="frame-{{frameIndex}}-parent" name="frame-{{frameIndex}}-parent" value="{{parent}}">]</label>
                    {{/return}}
                    {{^return}}
                    <label class="frameHeader">Global frame</label>
                    {{/return}}
                    <table class="stackFrameVarTable">
                    {{#var}}
                      <tr class="variableTr removable" id="frame-{{frameIndex}}-var-{{varIndex}}">
                        <td class="">{{#show_controls}}<button class="btn removeButton" type="button"></button>{{/show_controls}}</td><td class="stackFrameVar"><input class="stackFrameVarInput pl-html-input" id="frame-{{frameIndex}}-var-{{varIndex}}-name" name="frame-{{frameIndex}}-var-{{varIndex}}-name" value="{{name}}"></td>
                        <td class="stackFrameValue">
                          <div class="valueContainer">
                            <input class="stackFrameValueInput pl-html-input" id="frame-{{frameIndex}}-var-{{varIndex}}-val" name="frame-{{frameIndex}}-var-{{varIndex}}-val" value="{{val}}">
                            <button class="btn pointerValueButton" type="button"></button>
                          </div>
                        </td>
                      </tr>
                    {{/var}}
                    {{#return}}
                    <tr class="variableTr returnValueTr">
                    <td class=""></td>
                    <td class="stackFrameVar"><span>Return Value</span></td>
                    <td class="stackFrameValue"><input class="stackFrameValueInput pl-html-input" id="frame-{{frameIndex}}-return-val" name="frame-{{frameIndex}}-return-val" value="{{val}}"></td>
                    {{/return}}
                    </tr></table>{{#show_controls}}<button class="btn addVarButton" type="button">Add Variable</button>{{/show_controls}}</div>
                {{/frame}}
            </div>
            {{#show_controls}}<button type="button" class="btn" id="addFrameButton">Add Frame</button>{{/show_controls}}
            </td>
            <td id="heap_td">
              <div id="heap">
                <div id="heapHeader">Objects</div>
                {{#heap}}
                  {{#func}}
                    <div class="topLevelHeapObject removable" id="heap-func-{{funcIndex}}">
                      <div class="heapObject">
                        <div class="typeLabel">function</div>
                        <div class="funcObj">
                          func 
                          <input class="funcNameInput pl-html-input" id="heap-func-{{funcIndex}}-name" name="heap-func-{{funcIndex}}-name" value="{{name}}">
                          <span> [Parent = <input class="frameParentHeader pl-html-input" id="heap-func-{{funcIndex}}-parent" name="heap-func-{{funcIndex}}-parent" value="{{parent}}">]</span>
                        </div>
                      </div>
                      {{#show_controls}}<button class="btn removeButton" type="button"></button></div>{{/show_controls}}
                  {{/func}}
                {{/heap}}
            </div>
            {{#show_controls}}
              <button type="button" class="btn" id="addFuncButton">Add function</button>
              <button type="button" class="btn" id="addListButton">Add list</button>
            {{/show_controls}}
            </td>
        </tr>
        </tbody></table>
      </div></div>
      {{#pointer}}
        <div class="pointerArrow pointerTo-{{destination}}" id="pointer-{{origin}}">
          <svg xmlns="http://www.w3.org/2000/svg" 
          width="{{width}}" height="{{height}}" viewBox="0 0 {{width}} {{height}}" style="top: {{top}}; left: {{left}}"><path d="{{path}}"></path></svg>
          <input class="pointerInput pl-html-input" id="pointer-{{origin}}-display" name="pointer-{{origin}}-display" value="{{display}}">
        </div>
      {{/pointer}}
      </div>
<p>This tool is based on Python Tutor: <a href="https://pythontutor.com/">pythontutor.com</a></p>
</body>
</html> 